# Copia a .env.e2e o añade a .env.local para pruebas E2E

# URL de la app en desarrollo
E2E_BASE_URL=http://localhost:5176

# Service role (Dashboard → API) — confirma correos y evita rate limit
# E2E_SUPABASE_SERVICE_ROLE_KEY=

# Forzar registro solo por UI (sin admin)
# E2E_SKIP_UI_SIGNUP=0
